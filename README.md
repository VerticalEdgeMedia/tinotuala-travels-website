# Tino Tuala Travels

Static site, served by Cloudflare Workers from `public/`. No build step.

Preview: open `public/index.html` in a browser, or `npx wrangler dev`.
Deploy: push to `main`; Cloudflare builds and deploys from the connected repo.
